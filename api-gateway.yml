zuul:
  prefix: /api
  ignoredServices: '*'
  routes:
    projects-service: /project/**
    iterations-service: /iteration/**
    api-gateway: /gateway/**

ribbon:
  eureka:
    enabled: false

eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery-server:8761/eureka/
